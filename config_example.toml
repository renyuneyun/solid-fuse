pod = "https://solidos.solidcommunity.net/"
idp = "https://solidcommunity.net"

# auth = "css"   # OAuth2 client credentials + DPoP (default when username/password set)
# auth = "nss"   # Legacy NSS username/password login
# auth = "oidc"  # Browser-based OIDC flow (default when no username/password)

# username = "your-user-name"  # For css: client_id; for nss: username
# password = "your-password"   # For css: client_secret; for nss: password
